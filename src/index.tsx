import React from 'react';
import ReactDOM from 'react-dom';
import './test.css';

ReactDOM.render(
  <React.StrictMode>
    <h1>Hello, world!</h1>
  </React.StrictMode>,
  document.getElementById('root'),
);
